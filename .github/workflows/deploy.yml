name: Deploy to Vercel

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: |
          curl -s -X POST \
            "https://api.vercel.com/v1/integrations/deploy/prj_6L53sWrE1IW89n2ROZAU6BaSiRnk/yd82QnsOcP"
